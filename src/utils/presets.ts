import { TrackConfig } from '../types/track';

export const EMPTY_TRACK: TrackConfig = {
  id: 'user-circuit',
  name: 'My Custom Circuit',
  nodes: [],
  isClosed: false,
  defaultWidth: 14,
  asphaltColor: '#1C2129'
};
